# v2 PostgreSQL database

`schema.sql` creates the PostgreSQL structure.
`data.sql` imports the current data from the MySQL `muzik.sql` dump.

Run schema first, then data, in the Supabase SQL Editor.
